---
description: One-time (re-runnable) interview that builds config/ — the complete config layer /analysis and /dashboard depend on. Conversational, one topic at a time, nothing written until the user confirms.
argument-hint: "[section to update, if re-running]"
---

Read and follow `skills/setup/SKILL.md` in full, exactly as written —
that file is the actual instructions for this command, kept alongside
the engine it configures rather than duplicated here. Do not summarize
or skip steps.

Arguments given with this command (a section name if re-running an
already-configured company, otherwise empty for a first-time run):
$ARGUMENTS
