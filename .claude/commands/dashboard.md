---
description: Turn an entity's analysis into a polished, brand-styled, self-contained HTML dashboard for the GM/CFO/board — and recommend how to get it in front of them.
argument-hint: "[period] [for <entity>[, <entity>...]]"
---

Read and follow `skills/dashboard/SKILL.md` in full, exactly as
written — that file is the actual instructions for this command, kept
alongside the engine it drives rather than duplicated here. Do not
skip the deployment-tier recommendation step at the end.

Arguments given with this command (period, and optionally which
entities — see the skill's own scope-resolution rules for what an
empty argument means):
$ARGUMENTS
