---
description: Turn this month's ERP exports in inbox/ into a per-entity analysis — deterministic Python computes every number, Claude writes the narrative using the company's own tone, terminology, and investigation heuristics from config/.
argument-hint: "[period] [for <entity>[, <entity>...]]"
---

Read and follow `skills/analysis/SKILL.md` in full, exactly as written —
that file is the actual instructions for this command, kept alongside
the engine it drives rather than duplicated here. Do not summarize or
skip steps, including the narrative-writing step.

Arguments given with this command (period, and optionally which
entities — see the skill's own scope-resolution rules for what an
empty argument means):
$ARGUMENTS
