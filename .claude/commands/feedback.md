---
description: Add reviewer comments to dashboard rows, or edit/remove the LLM-written narrative for a flagged item — after /dashboard has rendered, in plain language, re-rendering the dashboard when done.
argument-hint: "<what to add/change> [for <entity> [period]]"
---

Read and follow `skills/feedback/SKILL.md` in full, exactly as
written — that file is the actual instructions for this command, kept
alongside the engine it drives rather than duplicated here. In
particular, don't skip the step-4 confirmation before writing anything.

Arguments given with this command (what to add/change, and optionally
which entity/period):
$ARGUMENTS
